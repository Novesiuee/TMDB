import {
  View,
  Text,
  StyleSheet,
  Image,
  StatusBar,
  Dimensions,
  FlatList,
} from "react-native";
export default function Home({ navigation }) {
  return;
}

const style = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "red",
  },
});
